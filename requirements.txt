# ============================================
# OpenMind LLM - Requirements
# ============================================

# Core Deep Learning
torch>=2.1.0
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.14.0
accelerate>=0.24.0

# Distributed Training
deepspeed>=0.12.0

# Experiment Tracking
wandb>=0.16.0

# API Server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Utilities
numpy>=1.24.0
tqdm>=4.66.0
pyyaml>=6.0.1
sentencepiece>=0.1.99
protobuf>=4.24.0
regex>=2023.10.3

# Serving (optional, for vLLM backend)
# vllm>=0.2.0

# Fine-tuning
peft>=0.6.0
trl>=0.7.0

# Quantization
bitsandbytes>=0.41.0

# Flash Attention (install separately: pip install flash-attn --no-build-isolation)
# flash-attn>=2.3.0

# CLI
typer>=0.9.0
rich>=13.7.0

# Evaluation
scikit-learn>=1.3.0

# Data Pipeline
fasttext-wheel>=0.9.2
datasketch>=1.6.0
xxhash>=3.4.0

# Testing
pytest>=7.4.0
httpx>=0.25.0

# ── HF Spaces Deployment ──────────────────────────────────────
# Weight download from Hugging Face Model Hub
huggingface_hub>=0.20.0
# Rate limiting for FastAPI (slowapi)
slowapi>=0.1.9
# Environment variable loading from .env
python-dotenv>=1.0.0
